# Agent/AI Instructions (Claude / Antigravity)

**Important Notice:**
The author will update the `project-description-raw.md` file to add new features in the future. Please refer to it whenever new features or context are needed.

**Critical Rule for Execution:**
For any future sections, command execution, or environment setups, you MUST always use `python` and `pip` from the local virtual environment (`.venv`).

Examples:
- DO NOT use `pip install x`, use `./.venv/bin/pip install x` (or `source .venv/bin/activate && pip install x`).
- DO NOT use `python script.py`, use `./.venv/bin/python script.py`.
